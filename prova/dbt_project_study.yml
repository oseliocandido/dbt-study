name: prova
version: '1.0.0'
config-version: 2

profile: 'sales-dbt'

model-paths: ['models']
seed-paths: ['seeds']
analsys-paths: ['analysis']
test-paths: ['tests']
snapshot-paths: ['snapshots']
macro-paths: ['macros']

docs-path: docs
asset-path: assets

target-path: target 
log-path: logs

clean-targetgs:
  - target
  - dbt_packages

packages-install-path: dbt_packages


quoting:
  database: true
  schema: true  
  identifier: true


models:
seeds:
snapshots:
analysis:
tests:
sources:
vars:


on-run-start:
on-run-end:















